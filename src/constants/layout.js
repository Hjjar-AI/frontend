/** Keep these values aligned with the breakpoint policy in assets/tokens.css. */
export const BREAKPOINTS = Object.freeze({
  compact: 360,
  narrow: 480,
  content: 640,
  navigation: 768,
  wide: 900,
})

export const LIST_SEARCH_DEBOUNCE_MS = 400
